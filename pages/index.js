import Hero from '../components/Hero';

export default function Home() {
  return (
    <>
      <Hero />
      {/* {images.map(image => (
        <img key={image.id} src={image.image} alt="" />
      ))} */}
    </>
  );
}
